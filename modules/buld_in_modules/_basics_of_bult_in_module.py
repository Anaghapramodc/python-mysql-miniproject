"""
bult in modules
modules which is defined aldredy
os:-deppent on file (operating system module)
sys:-system based module
platform
math:-mathamatical module
"""
import math
print(math.factorial(6))
print(dir(math))#list of function defined in the math module

import os
print(dir(os))

import sys
print(dir(sys))
print(sys.version)

